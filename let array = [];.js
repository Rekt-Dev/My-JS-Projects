                function alphabetPosition(text)

                {


                    console.log(`This is the text . length  ${text.length}`);

                    let array = [];
                    for (i = 0; i < text.length; i++) {
                        let letter = '';



                        if (text[i] === 'a') {
                            letter = '1';
                            console.log(array)
                            array.push(letter);

                        };

                        if (text[i] === 'b') {
                            letter = '2';
                            array.push(letter);
                        };

                        if (text[i] === 'c') {
                            letter = '3';
                            array.push(letter);
                            console.log('this is from within c')
                        };

                        if (text[i] === 'd') {
                            letter = '4';
                            array.push(letter);
                        };

                        if (text[i] === 'e') {
                            letter = '5';
                            array.push(letter);
                        };

                        if (text[i] === 'f') {
                            letter = '6';
                            array.push(letter);
                        };

                        if (text[i] === 'g') {
                            letter = '7';
                            array.push(letter);
                        };

                        if (text[i] === 'h') {
                            letter = '8';
                            array.push(letter);
                        };

                        if (text[i] === 'i') {
                            letter = '9';
                            array.push(letter);
                        };

                        if (text[i] === 'j') {
                            letter = '10';
                            array.push(letter);
                        };

                        if (text[i] === 'k') {
                            letter = '11';
                            array.push(letter);
                        };

                        if (text[i] === 'l') {
                            letter = '12';
                            array.push(letter);
                        };

                        if (text[i] === 'm') {
                            letter = '13';
                            array.push(letter);
                        };

                        if (text[i] === 'n') {
                            letter = '14';
                            array.push(letter);
                        };

                        if (text[i] === 'o') {
                            letter = '15';
                            array.push(letter);
                        };

                        if (text[i] === 'p') {
                            letter = '16';
                            array.push(letter);
                        };

                        if (text[i] === 'q') {
                            letter = '17';
                            array.push(letter);
                        };

                        if (text[i] === 'r') {
                            letter = '18';
                            array.push(letter);
                        };

                        if (text[i] === 's') {
                            letter = '19';
                            array.push(letter);
                        };

                        if (text[i] === 't') {
                            letter = '20';
                            array.push(letter);
                        };

                        if (text[i] === 'u') {
                            letter = '21';
                            array.push(letter);
                        };

                        if (text[i] === 'v') {
                            letter = '22';
                            array.push(letter);
                        };

                        if (text[i] === 'w') {
                            letter = '23';
                            array.push(letter);
                        };

                        if (text[i] === 'x') {
                            letter = '24';
                            array.push(letter);
                        };

                        if (text[i] === 'y') {
                            letter = '25';
                            array.push(letter);
                        };

                        if (text[i] === 'z') {
                            letter = '26';
                            array.push(letter);
                        };

                        console.log(letter)


                        console.log(`this is the array ${array}`);


                        console.log(`this is the letter variable ${letter}`)


                        let stringArray = array.toString()

                        console.log(stringArray)

                        console.log(array.join(' '))
                    }
                }

                alphabetPosition('josephhhhhhh');