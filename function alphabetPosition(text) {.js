function alphabetPosition(text) {

    let array = [];

    console.log(`This is the text . length  ${text.length}`);
    for (i = 0; i < text.length; i++) {
        const letter = '';


        if (text[i] === 'a'); {
            letter == '1';
            array.push(letter);
            array = array.concat(letter)
        };

        if (text[i] === 'b'); {
            letter == '2';
            array = array.unshift(letter);
        };

        if (text[i] === 'c'); {
            letter == '3';
            array = array.unshift(letter);
        };

        if (text[i] === 'd'); {
            letter == '4';
            array = array.unshift(letter);
        };

        if (text[i] === 'e'); {
            letter == '5';
            array = array.unshift(letter);
        };

        if (text[i] === 'f'); {
            letter == '6';
            array = array.unshift(letter);
        };

        if (text[i] === 'g'); {
            letter == '7';
            array = array.unshift(letter);
        };

        if (text[i] === 'h'); {
            letter == '8';
            array = array.unshift(letter);
        };

        if (text[i] === 'i'); {
            letter == '9';
            array = array.unshift(letter);
        };

        if (text[i] === 'j'); {
            letter == '10';
            array = array.unshift(letter);
        };

        if (text[i] === 'k'); {
            letter == '11';
            array = array.unshift(letter);
        };

        if (text[i] === 'l'); {
            letter == '12';
            array = array.unshift(letter);
        };

        if (text[i] === 'm'); {
            letter == '13';
            array = array.unshift(letter);
        };

        if (text[i] === 'n'); {
            letter == '14';
            array = array.unshift(letter);
        };

        if (text[i] === 'o'); {
            letter == '15';
            array = array.unshift(letter);
        };

        if (text[i] === 'p'); {
            letter == '16';
            array = array.unshift(letter);
        };

        if (text[i] === 'q'); {
            letter == '17';
            array = array.unshift(letter);
        };

        if (text[i] === 'r'); {
            letter == '18';
            array = array.unshift(letter);
        };

        if (text[i] === 's'); {
            letter == '19';
            array = array.unshift(letter);
        };

        if (text[i] === 't'); {
            letter == '20';
            array = array.unshift(letter);
        };

        if (text[i] === 'u'); {
            letter == '21';
            array = array.unshift(letter);
        };

        if (text[i] === 'v'); {
            letter == '22';
            array = array.unshift(letter);
        };

        if (text[i] === 'w'); {
            letter == '23';
            array = array.unshift(letter);
        };

        if (text[i] === 'x'); {
            letter == '24';
            array = array.unshift(letter);
        };

        if (text[i] === 'y'); {
            letter == '25';
            array = array.unshift(letter);
        };

        if (text[i] === 'z'); {
            letter == '26';
            array = array.unshift(letter);
        };



        console.log(`this is the array ${array}`);


        console.log(`this is the letter variable ${letter}`)
    }
}




alphabetPosition('ABCDdcbe');